"""Memory module — long-term persistent knowledge.

Stores preferences, conversations, tasks, notes, and ideas in SQLite
(metadata) and ChromaDB (semantic vectors). Implements
``core.interfaces.memory``.
"""
