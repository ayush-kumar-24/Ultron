"""Port interfaces (abstract contracts).

Each module exposes and consumes capabilities only through these ABCs /
Protocols. Implementations live in ``maira.modules`` (use cases) and
``maira.infrastructure`` (adapters).
"""
