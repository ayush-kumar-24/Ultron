"""Public plugin SDK — stable API surface for external plugin authors.

Defines hook decorators, context objects, and versioned contracts that
plugins may depend on. Breaking changes require a major SDK bump.
"""
